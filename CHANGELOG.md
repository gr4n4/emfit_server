# 변경 이력 (Changelog)

Emfit 침대 센서 데이터 수집 서버의 버전 이력.
[SemVer](https://semver.org/lang/ko/) 규칙: `MAJOR.MINOR.PATCH`
- **MAJOR**: 기존 사용 방식이 깨지는 변경
- **MINOR**: 기능 추가 (기존 사용법은 유지)
- **PATCH**: 버그 수정·자잘한 UI 수정

---

## [3.19.1] - 2026-09-10
- 🐛 `emfit-discord-bot.service`와 `emfit.service`를 거의 동시에 재시작하면, 대시보드가
  600MB+ 로그를 파싱하는 동안 봇이 `/internal/discord/status` 조회에 실패해 시설별
  슬래시 명령어(`/A시설` 등)를 하나도 못 만든 채 시작해버리던 문제 수정
  - `_register_facility_commands()`가 실패하면 바로 포기하지 않고 3초 간격으로 최대
    20회(약 1분) 재시도 — 그래도 안 되면 포기하고 로그에 남김(`/현황`·`/상세보기`는 항상 등록됨)

## [3.19.0] - 2026-09-10
- **디스코드 봇 시설별 슬래시 명령어** — `/admin/discord`에 등록해둔 채널(시설) 이름으로
  `/A시설`, `/B시설`처럼 그 시설 기기만 걸러 보여주는 명령어를 자동 생성
  - `GET /internal/discord/status` 응답에 기기별 `channel` 배정과 전체 `channels` 목록 추가
  - `discord_bot.py`가 시작할 때(`on_ready`) 이 목록을 읽어 시설 개수만큼 명령어를 동적으로 만듦
  - 새 시설을 추가/삭제하면 봇 재시작 한 번으로 명령어 목록도 같이 갱신됨
  - 기존 `/현황`(전체 요약)·`/상세보기`(전체 목록)는 그대로 유지

## [3.18.2] - 2026-09-01
- 🐛 디스코드 끊김 알림 기준을 "통신 시간 경과"가 아니라 "기기가 직접 보낸 끊김 신고"로 고정
  - 기존: 마지막 통신 시간이 오래되면 디스코드가 끊김으로 판단할 수 있어, 대시보드 조회/로그 파싱 타이밍에 따라 여러 기기가 한꺼번에 끊김·복구처럼 보일 수 있었음
  - 수정: EMFIT QS·AI Radar 는 payload 의 `connected` 값을, FSR 은 `fault/disconnect` 계열 이벤트를 기준으로 알림 판단
  - McKare 처럼 끊김 flag 가 없는 기기는 무소식만으로 디스코드 알림을 만들지 않음
  - 센서 수신 직후 `analyzer` 현재 상태 캐시를 즉시 갱신해, 대시보드를 열지 않아도 알림 기준이 최신 상태를 보게 함
  - Discord Webhook 전송 성공 시에만 끊김/복구 알림 상태를 저장하도록 수정
- 🧩 NRCarec 레이더 경보의 호실/이름 하드코딩 제거
  - AI Radar 기기의 `/devices` 배정 정보에서 위치와 사용자명을 읽어 `room`/`patientName` 으로 전송
  - 이름에 `421호 홍길동`처럼 함께 적어도 호실 번호를 분리해 알림에 사용

## [3.18.1] - 2026-08-26
- 🐛 EMFIT QS·AI Radar 디스코드 알림 오탐 수정 — 대시보드와 다른 기준을 쓰고 있었음
  - 기존: 하트비트(`status_at`) 수신 시각이 기준(threshold_minutes)보다 오래되면 무조건 '끊김'
  - 문제: 이 두 기기 종류는 하트비트 자체가 대시보드의 '부재'(침대 위에 없음) 상태일 때 뜸하게 옴 —
    실제로는 멀쩡히 연결돼 있는데도 '끊김'으로 잘못 알림이 감 (병동 실증에서 실측 확인)
  - 수정: 대시보드와 동일하게 EMFIT/AI Radar 는 하트비트에 실려 오는 `connected`(true/false) 값을 그대로 신뢰
  - 다만 하트비트 자체가 6시간 넘게 안 오면 그 마지막 값(설령 true 여도)을 믿지 않고 끊김으로 판단 —
    실측 결과 126일째 응답 없는 기기 중 다수가 `connected: true` 로 멈춰 있었음 (하트비트 수신 경로 자체가
    끊기면 이 필드도 갱신이 안 됨). 이 안전장치가 없으면 그런 기기는 영영 알림이 안 갔을 것

## [3.18.0] - 2026-08-26
- **디스코드 알림 오탐 방지(디바운스)** — 기준 시간을 넘겨 '끊긴 것 같다'고 감지해도 바로 알림을 보내지 않고,
  같은 시간만큼 더 지켜봐서 그래도 복구가 안 되면 그때 알림을 보냄 (총 대기 ≈ 기준×2)
  - 하트비트가 살짝 지연됐다가 곧바로 정상화되는 경우, 끊김→복구 알림이 연달아 뜨던 문제(양치기 소년) 방지
  - 확인 대기 상태(`_discord_pending`)는 메모리에만 둠 — 재시작으로 날아가도 최악의 경우 확인이 한 번 늦어질 뿐,
    거짓 알림 쪽으로는 절대 안 새는 방향이라 굳이 파일로 저장하지 않음
- **알림 메시지에 기기 종류 표시** — 메시지 맨 앞에 `(EMFIT QS)`, `(AI Radar)`, `(McKare)`, `(돌봄기기 사용 감지)` 붙임
  - 병동처럼 사용자명이 겹치는 곳에서 어느 기기가 끊겼는지 메시지만 보고 구분하기 위함

## [3.17.0] - 2026-08-26
- **디스코드 알림 시설별 채널 분리** — 기기마다 다른 Discord 채널(Webhook)로 알림을 보낼 수 있게 됨
  - `/admin/discord`에 "시설별 채널" 카드 추가 — 채널 이름 + Webhook URL을 등록 (`POST /admin/discord/channels`)
  - 기기 목록에 "보낼 채널" 드롭다운 추가 — 기기별 배정은 `POST /admin/discord/devices`(기존 `/admin/discord/thresholds`에서 이름 변경, 알림 기준과 채널 배정을 함께 저장)
  - 배정 안 한 기기는 그대로 기본 채널(맨 위 Webhook)로 감 — 알림이 새지 않음
  - 채널을 지우거나 이름을 바꾸면, 그 채널을 참조하던 기기 배정은 자동으로 기본 채널로 되돌림(참조 끊김 방지)
  - 목적: 시설(A시설/B시설/C시설)마다 담당자가 달라, 본인 시설 알림만 받게 하기 위함

## [3.16.0] - 2026-08-25
- **디스코드 알림 기기별 임계값** — `/admin/discord` 기기 목록에 기기마다 다른 '알림 기준(분)'을 넣을 수 있는 칸 추가
  - 보고 주기가 원래 긴 기기(예: 30분 간격으로 자체 하트비트를 보내는 게이트웨이 `gw01`)가 전역 기준과 거의 겹쳐서
    끊김↔복구를 반복하던 문제 — 그 기기만 여유 있는 값을 넣어서 해결
  - `POST /admin/discord/thresholds` — 기기별 값 저장 (빈 칸이면 전역 기본값 사용)
  - 🐛 `POST /admin/discord/save`(웹훅 URL·기본 임계값 저장)가 기기별 설정을 통째로 지워버리던 버그도 같이 수정

## [3.15.1] - 2026-08-25
- 🐛 디스코드 끊김 알림 상태(`_discord_alerted`)를 메모리에만 두던 것을 `discord_alert_state.json`에 저장하도록 수정
  - 안 그러면 서버 재시작(배포·크래시 등)마다 '이미 끊겨있던 기기'를 새로 끊긴 걸로 착각해 알림을 중복으로 보냄 — 실제로 이번 배포 과정에서 여러 번 재시작하며 알림이 과도하게 왔던 원인

## [3.15.0] - 2026-08-24
- **디스코드 연결 끊김 알림** — 기기가 일정 시간 통신 없으면 Discord Webhook으로 알림
  - `GET/POST /admin/discord` — Webhook URL·끊김 판정 기준(분)·켜기/끄기 설정 화면 (기기별 현재 상태 목록 포함, 언제든 웹에서 수정)
  - `POST /admin/discord/test` — 테스트 알림 전송
  - `POST /admin/discord/baseline` — 처음 켤 때 이미 끊겨있던 기기는 무시하고, 그 이후 새로 끊기는 기기만 알림 가도록 기준점 찍기
  - 1분 주기 백그라운드 감시(`_discord_monitor_loop`) — 상태가 바뀔 때만 알림(끊김 1회·복구 1회)이라 스팸 없음
- **디스코드 봇 슬래시 명령어** — `discord_bot.py`(별도 프로세스) 신규. `/현황`(연결·끊김 요약) · `/상세보기`(기기 전체 목록)
  - `GET /internal/discord/status` — 봇이 쓰는 localhost 전용 내부 상태 API. 대시보드가 이미 메모리에 들고 있는 상태를 그대로 내려줘서, 봇 하나 띄우자고 로그를 다시 파싱하지 않음
- 🐛 Windows(cp949 콘솔)에서 한글 로그의 em dash(—) 등 특수문자 때문에 서버 시작이 바로 죽던 `UnicodeEncodeError` 수정 — stdout/stderr UTF-8 강제

## [3.14.0] - 2026-08-24
- **FSR 노드 설정 페이지** — 하트비트 기반 원격 설정(사용자가 버튼을 누르지 않아도 노드가 깨어날 때 반영)
  - `GET /fsr-nodes` — 노드별 임계/해제비율/타점 등을 설정하는 관리자 화면
  - `GET /api/fsr/nodes` — 노드 목록·마지막 상태·대기 명령 API
  - `_fsr_last_update` — `POST /jy01` 이벤트로 노드 마지막 상태를 메모리에 추적
  - 대시보드에 'FSR 노드 설정' 메뉴 링크 추가
  - 기존 `/fsr-tune`(실시간 튜닝)은 그대로 — 실시간 조정용, 노드 설정은 비동기 반영용

## [3.13.0] - 2026-08-07
- **원격 FSR 튜닝 시스템** — 사용자 집 노드의 압력 민감도를 전화 통화하며 원격 조정
  - 게이트웨이가 공유기 뒤에 있어 서버가 먼저 접속 못 하므로 **폴링 구조**로 구현:
    게이트웨이가 `GET /jy01/cmd` 로 대기 명령을 CSV 한 줄로 가져가고 `POST /jy01/cmd/ack` 로 완료 보고
  - `POST /jy01/live` — FSR 모드 노드의 실시간 센서값(초당 1건, 메모리에만·30초 TTL, 파일 미기록)
  - `GET /fsr-tune` — 관리자용 실시간 튜닝 화면(센서 막대 + 임계/해제/캘리브레이션 명령)
  - `GET /api/fsr/live`·`POST /api/fsr/cmd` — 튜닝 화면 데이터/명령 API(관리자 인증, 값 범위 검증)
  - 명령은 노드당 항목별 1건만 대기(덮어쓰기), 한 번에 하나씩 전달해 적용 추적 가능
  - ⚠️ 딥슬립 노드에는 즉시 전달 불가 — 사용자가 노드 버튼 3초로 FSR 모드 진입 시에만 조정

## [3.12.0] - 2026-08-07
- ⚡ **이름·위치 수정이 즉시 반영되도록 개선 (수 분 → 즉시)**
  - 원인: 레코드에 사용자명이 **박혀서 저장**되기 때문에, 이름만 고쳐도
    `invalidate_cache()` 로 캐시를 통째로 버리고 로그 전체(젯슨 86MB)를 다시 읽었다.
    저장 버튼을 누르고 몇 분씩 기다려야 했던 이유
  - 착안: **이름 수정은 배정 경계(start/end)를 건드리지 않는다.** 어떤 레코드가 어느 배정에
    속하는지가 그대로이므로, 라벨만 갈아끼우면 재파싱과 결과가 같다
  - `relabel_assignment()` 신규 — 캐시에 올라간 레코드의 `사용자`/`위치` 만 그 자리에서 교체
  - 측정: 20만 건 합성 로그에서 **전체 재파싱 1.2초 → 라벨 갱신 0.07초**.
    젯슨 실제 로그는 훨씬 커서 체감 차이가 더 크다
  - 기기 이전(`handover`)처럼 **배정 경계가 바뀌는 경우엔 여전히 재파싱**한다 — 그때는 필요하다.
    `/devices` 저장도 새 배정이 생겼을 때만 재파싱
- **원본 데이터 화면에서 파일을 골라 볼 수 있게** (`/dashboard/raw`)
  - EMFIT QS · AI Radar · McKare · 사용감지 · McKare 이미지 목록 탭
  - 파일별 크기·최종 수신 시각 표시, 마지막 1/5/20/50줄 선택
  - ⚡ **파일을 통째로 읽지 않는다** — 예전엔 `readlines()` 로 86MB 를 매번 메모리에 올렸다.
    `_tail_lines()` 로 끝에서부터 필요한 만큼만 읽어, 파일이 아무리 커도 즉시 뜬다 (0.009초)
  - 볼 수 있는 파일은 목록으로 제한 — 경로를 밖에서 받지 않아 임의 파일 열람 불가
- **'측정 데이터 없음' → '설치됨 · 측정 대기'** (노란 경고색 → 중립 회색)
  - 설치 직후 아직 아무도 안 누운 정상 상태인데 경고처럼 보였다.
    통신은 되고 있으므로 이상이 아님을 표현. 부제로 '아직 측정된 사람이 없습니다' 추가

## [3.11.0] - 2026-08-07
- **그룹(view) URL 에 기기 추가·제외 기능** — `POST /admin/views/edit`
  - 지금까지는 그룹을 만들 때 고른 기기가 고정이라, 기기가 늘면 **새 URL 을 발급해
    받는 분들에게 다시 배포**해야 했다. 현실적이지 않다
  - ⭐ **토큰(=URL)은 그대로 두고 기기 목록만 바꾼다.** 이미 나간 주소를 계속 쓸 수 있다
  - `/admin/tokens` 의 각 그룹에 `✏️ 기기 추가/제외` 버튼 — 펼치면 체크박스 목록
    · 설치장소별로 묶고, 기기 종류를 색 태그로 표시 (대시보드 섹션과 같은 색)
    · 그룹 이름도 같이 수정 가능
    · 한 번에 하나만 펼쳐 화면이 복잡해지지 않게
  - 안전장치: 기기를 **0개로 만들 수 없다**(400). 그러면 그룹 대시보드가
    '접근할 수 있는 그룹이 없습니다'가 되어버린다 — 없앨 의도라면 폐기 버튼을 써야 한다
  - 수정 시 서버 로그에 추가·제외된 SN 을 남긴다
- 검증: A시설(EMFIT 4대) → 사용감지 추가 → **기존 URL 그대로 5대 표시** 확인.
  제외·이름 변경·안전장치·주요 화면 7개 회귀 없음

## [3.10.0] - 2026-08-07
- **기기 관리 화면 개편 — 검색 · 필터 · 장소별 묶기 · 숨기기** (`/devices`)
  - 기기가 늘수록 스크롤이 길어지던 문제를 네 가지로 나눠 해결

    | 기능 | 해결하는 것 |
    |---|---|
    | 🔍 검색 | 이름·위치·SN 으로 즉시 추림 |
    | 종류 필터 칩 | EMFIT / Radar / McKare / 사용감지 별로 보기 |
    | 📍 장소별 묶기 | 같은 설치장소 기기가 붙어 있어 훑기 쉬움 (`미지정`은 맨 뒤) |
    | 👁 숨기기 | 안 쓰는 기기를 대시보드 카드에서 감춤 |

  - 검색·필터는 **화면 안에서만** 걸러낸다 — 서버 왕복도, 저장도 필요 없다.
    장소 머리글은 그 아래 기기가 전부 걸러지면 같이 숨는다
  - 기기 종류를 색 태그로 표시 — 대시보드 섹션과 같은 색이라 어디 기기인지 바로 안다
- **숨기기는 삭제가 아니다** — 대시보드 카드에서만 빠지고
  데이터·리포트·상세페이지·배정이력·기기관리 화면에는 그대로 남는다. 언제든 되돌린다
  - `assignments.json` 에 `hidden: true` 로 저장. 끄면 키를 지워 파일에 `false` 가 안 쌓이게 함
  - 기기 이전(`handover`) 시 숨김 상태를 물려받는다 — 같은 실물 기기이므로
  - ※ **삭제 버튼은 만들지 않았다.** 연구 데이터가 든 화면에 실수로 누르면
    되돌릴 수 없는 버튼을 두지 않는 편이 안전하다
- 검증: 숨김 → 카드 2종에서 제외 확인 → 해제 → 복귀. 기기관리·상세페이지·배정이력 보존 확인.
  4종 센서 수신·주요 화면 7개 회귀 없음

## [3.9.2] - 2026-08-07
- 🔴 **서버 시간대에 따라 끊긴 기기가 '정상'으로 보이던 버그 fix**
  - 원인: 카드가 `datetime.now()`(서버 로컬, tz 없음)와 `시간(KST)`(KST 기준)을 직접 뺐다.
    서버가 KST 라 지금까지 우연히 맞았을 뿐이다
  - 실제 확인: **5시간 전에 끊긴 기기**를 서버 시간대별로 렌더해 봄

    | 서버 | 표시 (수정 전) | 수정 후 |
    |---|---|---|
    | KST | 부재 | 부재 |
    | **UTC** | **재실 · "방금"** 🔴 | 부재 |
    | UTC+12 | 부재 | 부재 |

  - ⚠️ 돌봄 관제에서 **죽은 장비가 살아있는 것처럼 보이는 것**이 가장 위험하다.
    젯슨을 옮기거나 도커로 감싸면 바로 발생한다
  - 수정: `_state_dt()` 신규 — 측정 시각을 KST 인식(aware) 으로 파싱.
    카드용 `now` 도 `datetime.now(KST)` 로 통일. 세 시간대에서 결과 동일 확인
- 🔴 **기기 이름·위치에 넣은 HTML 태그가 실행되던 문제 fix (XSS)**
  - v1 카드는 이름·위치 둘 다, **v2 카드는 위치가** 이스케이프 없이 삽입되고 있었다
    (3.5.0 때 "v2 는 안전하다"고 본 것은 이름만 확인한 오판)
  - 경로가 실재한다: `/devices/edit_user` 는 **그룹(view) 토큰 보유자도** 호출할 수 있어,
    외부 관람자가 넣은 문자열이 관리자 대시보드에서 실행될 수 있었다
  - 수정: 삽입 지점이 아니라 **값을 만드는 지점**에서 이스케이프.
    새 화면을 만들 때 또 빠뜨리지 않도록 (`location_text`/`loc`/`name_safe`)
  - 카드 5종(v1·v2·비활성 2종·사용감지) 전부 공격 재현으로 차단 확인

## [3.9.1] - 2026-08-07
- **서비스 이름 변경** — `Emfit QS 대시보드` → **`돌봄기기 통합 대시보드`**
  - 기기가 4종(EMFIT QS·AI Radar·McKare·사용감지)으로 늘어 더 이상 Emfit 전용이 아니다
  - 브라우저 탭 제목 10곳, 로그인·안내·사용 가이드 제목, 하단 버전 표기 전부 교체
  - ⚠️ **기기 종류를 가리키는 `EMFIT QS` 는 그대로 둔다** — 대시보드 섹션명, CSV 파일명,
    사용 가이드의 기기별 설명 등. 그건 실제 제품 이름이라 바꾸면 오히려 틀린 말이 된다
  - 사용 가이드 본문도 4종 기준으로 보완:
    · 전송 주기 설명을 기기별로 분리 (EMFIT 30초 / Radar 1~50초 / 사용감지는 이벤트 시에만)
    · "HR/RR 이 '-' 로 뜬다" 항목에 사용감지는 생체값을 아예 안 잰다는 안내 추가

## [3.9.0] - 2026-08-06
- **McKare AI 110 열화상 이미지 수신 경로 추가** (`/data-receiver/device-image`)
  - ⚠️ JCFT 문서는 **VSR 22 기준이라 이미지 규격이 아예 없다.** AI 110 은 열화상이
    붙은 다른 모델이라 별도 규격이 있을 텐데 아직 받지 못했다.
    그래서 흔히 쓰이는 세 방식을 **모두** 받아둔다 — 규격을 몰라도 데이터를 잃지 않고,
    실제로 뭐가 들어오는지 보면 규격을 역으로 확인할 수 있다:
    1. `multipart/form-data` (파일 업로드 표준, 가장 가능성 높음)
    2. `application/json` + base64 문자열 (`data:` 접두어도 처리)
    3. 원시 바이너리 (`Content-Type: image/jpeg` 등)
  - 형식은 Content-Type 이 아니라 **파일 시그니처**로 판별 (JPEG/PNG/BMP/WebP/GIF).
    Content-Type 을 못 믿는 장비가 있어서
  - 기기 식별자 필드명을 모르므로 폼·JSON·헤더·쿼리에서 흔한 후보를 모두 훑는다
    (`macAddress`/`mac`/`deviceId`/`serial` …)
  - ⚠️ **이미지는 jsonl 에 넣지 않는다.** 한 장 10KB 라도 1분마다면 하루 14MB, 1년 5GB 다.
    측정값 로그에 섞으면 대시보드가 읽을 파일이 무거워져 조회가 통째로 느려진다.
    파일은 `mckare_images/날짜/` 에 두고, `mckare_image_log.jsonl` 에는 목록만 남긴다
    (이 목록은 `DATA_FILES` 에 넣지 않아 파싱 대상이 아니다)
  - 한 장 10MB 상한 (`413`). 함께 온 필드는 전부 메타로 기록 — 규격 파악용
- **McKare 경로에 `/mckare` 접두어 형태 추가**
  - 문서의 `{mckare-api-address}` 가 '호스트만'인지 '경로 포함'인지 명시돼 있지 않다.
    펌웨어에 무엇을 넣든 닿도록 접두어 있는/없는 형태를 모두 연다 (측정값 5개·이미지 4개 경로)
- 검증: 3형식 수신·시그니처 판별·필드명 변형·10MB 차단·9개 경로 201 통과. 4종 센서 회귀 없음

## [3.8.1] - 2026-08-06
- **퇴역 기기가 안 지워지던 문제 fix** — `_RETIRED_SNS` 에 강제 제거 옵션 추가
  - 원인: 안전장치가 과했다. 이름·위치가 기본값 그대로일 때만 지우게 해뒀는데,
    옛 레이더(`A1B2C3D4E5F6`)는 위치가 `테스트 공간` 로 수정돼 있어 매번 건너뛰었다.
    로그에는 `수정 이력이 있어 그대로 둔다` 가 찍혔지만 화면만 보면 알 수 없었다
  - 수정: `_RETIRED_SNS` 값이 `None` 이면 이름·위치가 바뀌었어도 제거한다.
    사용자가 명시적으로 없애라고 한 기기에만 쓴다
  - 옛 레이더 2대·테스트 잔재 2건을 강제 제거로 전환 (모두 수신 데이터가 없어 잃을 기록 없음)
  - 다른 기기의 이름·위치는 그대로 보존됨을 확인

## [3.8.0] - 2026-08-06
- **AI Radar MAC 변경** `A1B2C3D4E5F7` → `A1B2C3D4E5F8` (설치 시 다른 실물이 옴, 모델 동일)
  - 보유 기기가 이 한 대뿐이므로 옛 두 대(`A1B2C3D4E5F6`, `A1B2C3D4E5F7`)는 `_RETIRED_SNS` 로 정리
- **McKare 표준 경로 추가** — JCFT 문서(JCFT-MCK-API-IM-001) 6장의
  `https://{base}/data-receiver/device-measurement` 형식을 그대로 흉내 낸다.
  센서 펌웨어가 이 경로로 쏘게 되어 있어 우리 쪽이 모양을 맞춰야 한다
  - 하이픈(문서 정식)·언더스코어 표기 둘 다 수신. 기존 `/mckare` 는 별칭으로 유지
  - 동작·응답(201 `{statusCode, message}`)·ApiKey 검증은 기존과 동일
  - 점검(워밍업) 중에도 수신되도록 게이트 예외에 추가
- **사용감지 카드 정렬 기준 신설** — 기존 규칙이 활동량(ACT) 기반이라
  생체값이 없는 사용감지에는 통하지 않았다(사실상 SN 순으로 나열됨).
  대시보드 전체 원칙과 동일하게 **손봐야 할 것을 위로**:
  `센서 확인 필요` → `사용 중` → `미사용` → `판정 대기`
- **테스트 잔재 정리** — `B1C2D3E4F5A6`(돌봄기기 1), `CONNECTIVITY-TEST` 제거
  - ⚠️ 퇴역 처리 시 두 가지 되살아남 경로를 함께 막았다:
    1. 로그 파일에 옛 데이터가 남아 있으면 `_ensure_fsr_device` 가 재등록 → 퇴역 목록은 등록 거부
    2. `_FSR_DEVICE_DEFAULTS` 에 남아 있으면 시드 루프가 재생성 → 시드 시 퇴역 목록 건너뜀
  - `_FSR_DEVICE_DEFAULTS` 를 비웠다. 실물 보드는 첫 데이터에 자동 등록되므로 미리 적을 필요가 없다
- 검증: 정리 4건·신규 레이더 수신·McKare 3경로·카드 정렬·4종 회귀 통과

## [3.7.0] - 2026-08-03
- **상세페이지에 '얼마나 오래' 통계 추가** — 상세페이지 시안에서 합의했던 항목들.
  3.6.0 에서 사용감지만 넣고 나머지 3종을 빠뜨린 것을 채운다. 디자인은 기존 V1 양식 그대로

  | 기기 | 추가된 블록 | 보여주는 것 |
  |---|---|---|
  | EMFIT | 🛏️ 침대 재실 시간 | 총 재실 시간 · 이탈 횟수 · 24시간 띠 |
  | AI Radar | 🧭 자세별 시간 | 자세마다 시간·비율·횟수 (누움/뒤척임/앉음/걸터앉음/배회/자리비움/낙상) |
  | McKare | 📍 구간 재실 시간 | 총 재실 시간 · 부재 횟수 · 띠 |
  | 사용감지 | (3.6.0 에서 완료) | 사용 구간 · 일별 사용시간 |

  - `_sampled_bands` 신규 — 셋이 공유하는 계산. Emfit·레이더·McKare 는 이벤트가 아니라
    '주기적으로 찍히는 값'이라, 각 표본이 다음 표본까지의 시간을 대표한다고 보고 길이를 매긴다
  - 측정이 끊긴 구간을 재실로 세지 않도록 기기별 최대 간격을 둔다
    (`_BAND_GAP_SEC` — Emfit 5분 / 레이더 2분 / McKare 5분). 이보다 벌어지면 구간을 끊는다
  - 머리말의 총합에서 '없는 시간'(이탈·부재·자리비움)은 빼고, 대신 횟수로 보여준다
  - 블록 기본 순서를 **통계 먼저**로 변경 — 화면을 열자마자 제일 먼저 알고 싶은 값이고,
    그래프는 그 뒤를 뒷받침하는 근거이기 때문.
    ※ 이미 순서를 바꿔 저장한 사용자는 그 배치가 유지되고 새 블록만 뒤에 붙는다
      (사람이 정한 배치를 코드가 되돌리지 않는다)
  - 검증: 합성 데이터로 기대값 대조 — EMFIT 재실 12.5h·이탈 2회, 레이더 자세별 시간,
    McKare 재실 7h·부재 1h 모두 일치. 대시보드·대시보드2·기기관리·리포트 회귀 없음
  - ※ 시안에 있던 **낙상 이벤트 목록**은 아직 안 넣었다 — 별도 작업

## [3.6.0] - 2026-08-03
- **상세페이지를 기기 종류별로 분리** (`/device/{sn}`, 기존 V1 양식 유지)
  - 지금까지는 모든 기기에 수면요약·심박·호흡·활동량 4개를 똑같이 그려서,
    그 값을 재지 않는 기기는 **빈 그래프가 남아 "고장인가?" 하고 헷갈렸다.**
    이제 안 재는 값은 블록 자체를 만들지 않는다

    | 기기 | 블록 |
    |---|---|
    | EMFIT | 수면요약 · 심박 · 호흡 · 활동량 (변화 없음) |
    | AI Radar | 심박 · 호흡 · **자세**(신규) |
    | McKare | 심박 · 호흡 · **체온**(신규) |
    | 사용감지 | **사용 구간 · 일별 사용시간**(신규) |

  - **사용 구간** — 선택한 기간을 가로 띠로 놓고 언제부터 언제까지 썼는지 표시.
    총 사용시간·횟수·비율 요약과 최근 8건 목록도 함께. 진행 중인 사용은 빗금으로 구분
  - **일별 사용시간** — 기록이 있는 최근 14일의 하루 총 사용시간 막대그래프
  - 사용 구간은 Chart.js 대신 직접 그린다 — 단순한 띠라 라이브러리보다 가볍고
    시간 축을 창 범위에 정확히 맞추기 쉽다
  - `_fsr_intervals` 신규 — press/release 를 구간으로 변환. 까다로운 경우를 다룬다:
    · 시작만 있고 종료 없음 → 아직 사용 중(지금까지)
    · 종료만 있고 시작 없음 → 보드가 준 `duration_ms` 로 시작을 역산
    · 역산이 앞 구간과 겹치면 잘라내 **이중 계산 방지** (일별 합계 정확도)
    · 생존신고 등 상태를 모르는 이벤트는 무시
  - ⚠️ 일별 합계에서 진행 중인 세션은 **자정이 아니라 '지금'까지**만 센다
    (자정까지로 계산하면 오늘 사용시간이 몇 시간씩 부풀어 오른다)
  - 자세 그래프는 계단식(stepped) + 눈금에 한글 라벨(누움/앉음/낙상…)
  - 블록 순서 환경설정(`ALL_BLOCK_IDS`)을 새 블록까지 포함하도록 확장.
    viewer 단위 설정이라 화면에 없는 블록 id 는 프런트가 그냥 무시한다
  - 검증: 4종 기기 상세페이지 렌더·시계열 API·구간 계산 11개 경계 케이스 통과.
    EMFIT 상세페이지·대시보드·기기관리·리포트 회귀 없음
  - ※ McKare 는 자동 기기 등록이 없어 `assignments.json` 에 없으면 상세페이지가 404.
    기존 동작이며 이번 변경과 무관 — 필요해지면 별도로 다룬다

## [3.5.1] - 2026-08-03
- **사용감지(FSR) 카드를 신규 대시보드(V2, `/dashboard2`) 섹션에 연결**
  - 3.5.0 에서 FSR용 V2 카드 함수는 있었으나 섹션 루프에 연결되지 않아 생체신호 카드로
    잘못 그려지던 것을 수정. 이제 사용 중/미사용/센서 확인 필요 + 배터리가 정상 표시된다.
  - 기존 `/dashboard`·`/view` 및 다른 기기(EMFIT·Radar·McKare) 표시는 변화 없음.

---

## [3.5.0] - 2026-08-03
- **AI Radar 기기 교체 — MAC 변경** `A1B2C3D4E5F6` → `A1B2C3D4E5F7` (같은 OUI `A1:B2:C3` = 라닉스)
  - 옛 기기는 제조사 사용 권장 중단 + 실제 수신 데이터 없음 → 배정 목록에서 자동 정리
  - `_RETIRED_SNS` 신규 — 퇴역 기기의 '기본 등록'을 1회성으로 제거한다.
    안 쓰는 기기가 빈 카드로 계속 남아 있으면 오히려 헷갈리기 때문
  - ⚠️ **사람이 손댄 흔적이 있으면 지우지 않는다** (`_is_untouched_default`).
    이름·위치를 고쳤거나 배정 이력이 갈라졌다면 그건 사람이 의미를 부여한 기록이라
    코드가 임의로 지울 대상이 아니다. 그 경우 로그로 알리고 화면에 남긴다
- **돌봄기기 사용감지 센서(ESP32 압력/FSR) 연동 — 수신 + 대시보드 표시** (4번째 기기 유형)
  - 자체 제작 센서를 돌봄기기에 부착해 **'지금 이 기기가 쓰이고 있는가'**를 본다.
    압력 감지 → `사용 중`, 압력 해제 → `미사용`. 생체신호는 재지 않는다
  - 대시보드 표시 항목: **사용자 · 설치장소 · 사용 중/미사용 · 배터리 잔량**
    (+ 직전 사용 시간, 마지막 신호 시각)
  - 전용 수신 경로 `POST /jy01` — 다른 센서 경로와 분리. `deviceId`·`event` 필수,
    누락 시 400. 저장 실패는 500 으로 알려 보드가 재전송할 수 있게 함
  - `fsr_parser.py` 신규 — `deviceId`/`event`/`duration_ms`/`battery_pct`/`battery_mv`/`uptime_ms` 정규화.
    event 표기 흔들림 흡수(press/pressed/down/on…, release/released/up/off…, heartbeat/alive/ping…)
  - ⚠️ **`uptime_ms` 는 타임스탬프로 쓰지 않는다** — 부팅 후 경과시간이라 절대시각이 아님.
    보드가 epoch(`ts`/`timestamp`/`time`/`epoch`…)를 보내면 그걸 쓰고(초·밀리초 자동 판별),
    없으면 McKare 처럼 서버 수신시각을 KST 로 해석
  - **배터리** 항목 신규 — 다른 센서엔 없던 값. 30% 이하 주황, 15% 이하 🪫 빨강
  - **상태는 딱 세 가지** — `사용 중` / `미사용` / `센서 확인 필요`
  - ⚠️ **`센서 확인 필요`는 근거가 있을 때만 띄운다.** 침묵은 근거가 아니다.
    하루 종일 기기를 안 쓸 수 있고, 서버는 '신호 없음'이 고장 때문인지 미사용 때문인지
    구분하지 못한다. 침묵으로 경고하면 거짓 경보가 반복돼 진짜 고장도 무시하게 된다.
    확실한 근거 셋으로만 판정한다:
    1. 펌웨어가 센서 이상을 직접 알림 (`event`: error/disconnect/unplugged/… → `fault`)
    2. 배터리 잔량이 `FSR_BATT_CRITICAL`(5%) 이하 — 사실상 방전
    3. **생존신고를 보내는 보드인데** 그마저 1시간 끊김 (`keepalive_seen` 일 때만 적용)
  - 3번은 그 보드가 실제로 생존신고를 보낸 적이 있어야만 작동한다.
    이벤트만 보내는 보드에는 시간 기준을 아예 적용하지 않는다 (조용함 ≠ 고장).
    오래 죽은 기기는 기존 7일 규칙이 '비활성 기기'로 따로 걸러준다
  - 이상 상태는 latch — 이상 알림 후 조용해져도 유지되고, 정상 이벤트가 와야 풀린다
  - 카드에 **사유**를 함께 표시 (`압력 센서 연결 확인` / `배터리 소진 (3%)` / `전원·통신 확인`)
  - 경과 판정은 문자열 시각이 아니라 epoch(`last_seen_ts`)로 계산해 서버 시간대와 무관하게 동작
  - 생존신고(heartbeat)가 마지막으로 들어와도 직전 사용/미사용 상태를 잃지 않도록
    `get_latest_states` 에서 확정 상태를 따로 보존해 합침
  - ⚠️ **기기 종류를 SN 모양으로 추측하지 않는다** — ESP32 의 MAC(`B1C2D3E4F5A6`)도
    12자리 16진수라 라닉스 레이더와 생김새가 같다. 배정 정보에 `kind` 를 저장하고
    (`_rebuild_device_info`·`handover`·`update_active_assignments` 가 보존)
    대시보드는 그 값으로 섹션을 정한다. 이래야 **데이터 도착 전**에도 올바른 섹션에 뜬다
  - `deviceId` 가 MAC 형태면 라닉스·McKare 와 동일하게 '구분자 없는 대문자'로 정규화 —
    펌웨어가 `A1:B2:C3:...` 로 보내다 `A1B2C3...` 로 바꿔도 카드가 갈라지지 않음
  - **미등록 보드 자동 등록** — `_ensure_fsr_device`. 등록 안 된 `deviceId` 로 데이터가 오면
    배정을 자동 생성해 대시보드에 바로 뜨게 함('데이터는 쌓이는데 화면엔 없음' 방지).
    `/jy01` 이 무인증이므로 ID 형식 검증 + 자동 등록 상한 20대로 남용 차단
  - 대시보드: 기존/신규(v2) 양쪽에 **'돌봄기기 사용 감지' 섹션** 추가. 심박·호흡 칸 대신
    사용 상태 + 배터리 + 직전 사용 시간 표시. 헤더 요약에 `사용감지 N대`
  - `_render_device_section` 에 `render` 인자 추가 — 표시 항목이 다른 기기도 같은 구역 틀 재사용
  - 원본 로그 `fsr_data.jsonl` 로 분리, `DATA_FILES`·`.gitignore`·점검중 수신 예외에 추가
  - FSR 은 생체값이 없어 HR/RR/ACT 컬럼을 만들지 않음 (CSV 에 빈 칸만 남는 것 방지)
  - 검증: 파서 8종·엔드포인트 6종·저장→최신상태→카드 렌더 전 경로 통과.
    Emfit(Live/Summary)·라닉스(BED/FALL)·McKare·하트비트 회귀 이상 없음
  - ※ 기기 상세페이지(`/device/{sn}`)의 그래프는 아직 HR/RR/ACT 기준이라
    FSR 기기는 상단 카드만 유효하고 그래프는 비어 보임 — 별도 작업 예정

## [3.4.1] - 2026-07-31
- **대시보드 반응 속도 개선 (데이터 누적으로 느려지던 문제)**
  - 원인: `get_latest_states` 가 최신 상태를 찾으려고 매번 전체 storage(약 69만 레코드)를 풀스캔.
    Emfit 데이터가 몇 초마다 들어와 캐시가 계속 무효화되므로, 15초 갱신·페이지 이동마다 반복됨
  - 수정: 최신 상태는 기기별 '최근 날짜' 버킷에만 있으므로 최근 2일치만 스캔 →
    합성 69만 레코드에서 206ms → 9ms (22배), 결과값은 기존과 100% 동일
  - `list_available_assignments`(리포트 페이지) 도 파일 mtime 기준 캐시 추가 (두 번째부터 즉시)

## [3.4.0] - 2026-07-29
- **McKare(JCFT VSR22) 연동 — 수신 경로 추가** (3번째 기기 유형)
  - 전용 수신 경로 `POST /mckare` — Emfit(`/`)·라닉스(`/radar`)와 분리
  - `mckare_parser.py` 신규 — VSR22 필드 정규화(macAddress/respirationDetection(재실 0~3)/
    fallDetection(0~2)/heartRate/respirationRate/activityDetection/temperature)
  - **체온(temperature)** 항목 신규 — Emfit·라닉스엔 없던 값
  - ⚠️ 성공 응답을 **201 Created** + `{statusCode:201, message:"created"}` 로 반환 —
    McKare 센서가 201 을 성공으로 판단하므로 200 을 주면 재전송을 유발함
  - (선택) `mckare_apikey.txt` 가 있으면 ApiKey 헤더 검증, 없으면 미적용
  - 원본 로그 `mckare_data.jsonl` 로 분리, `DATA_FILES`·`.gitignore` 에 추가
  - 라닉스 FALL 과 안 섞이도록 파서에서 pose 있으면 제외 (교차 오탐 0 확인)
  - 검증: /mckare 엔드포인트·파서·저장·최신상태 29개 테스트 통과, Emfit·라닉스 회귀 이상 없음
  - ※ 대시보드 전용 섹션·카드(체온 표시 등)는 실데이터 수신 후 별도 작업 예정

## [3.3.2] - 2026-07-27
- **로그 파싱 속도 8~14배 개선 (재시작 시 워밍업 시간 단축)**
  - 원인: `add_to_storage` 가 행마다 pandas Timestamp 를 만들고 tz 변환을 2번씩 함.
    398MB(약 73만 줄) 전체 파싱이 실서버에서 ~10분(메모리 부족 시엔 훨씬 더) 걸렸음
  - 수정: 시간 변환을 pandas → 표준 datetime 으로 교체 (핫패스). `_to_naive_kst`·`_parse_dt` 도
    datetime 기반으로 통일. 200,000줄 합성 데이터로 8.6배 확인, 결과 190,000건 baseline 과 100% 동일
  - 덤: 13자리 밀리초 타임스탬프가 pandas 범위 밖이라 조용히 버려지던 잠재 버그도 해소
  - 참고: 실행 중 증분 파싱은 원래 빨랐고, 이번 개선은 재시작(콜드 스타트) 전체 파싱에 특히 효과

## [3.3.1] - 2026-07-27
- **같은 초에 여러 레이더 기록이 들어오면 최신 자세가 갱신되지 않던 버그 fix**
  - 원인: `server_received_at` 이 초 단위라 같은 초에 들어온 기록끼리 시각이 동일한데,
    `get_latest_states` 가 `>` 로 비교해 "같은 초의 맨 처음 건"을 붙들고 있었음
    (재실 40건 → 낙상 → 자리비움 순으로 와도 카드는 계속 '재실'로 표시)
  - 수정: `>=` 로 비교 — 같은 초에서는 저장 리스트의 도착순 마지막(=가장 최근 도착)을 최신으로 택함
  - 더미 데이터 종합 검증 스크립트로 확인 (PDF 예시 수신, 잘못된 payload 방어,
    1초 주기 60건 연속 수신, 대시보드·CSV 반영 — 22개 항목 전부 통과)

## [3.3.0] - 2026-07-24
- **AI Radar (RANIX RMR602A) 연동**
  - 전용 수신 경로 `POST /radar` — Emfit(`POST /`)과 분리해 payload 혼동 방지
  - `radar_parser.py` 신규 — BED/FALL 두 형식 자동 판별, 자세 7종 매핑, MAC 정규화.
    Emfit 파서를 건드리지 않도록 별도 모듈로 분리
  - 대시보드를 **EMFIT QS / AI Radar 두 섹션**으로 분리, 낙상 카드는 최상단 정렬
  - 레이더 카드는 ACT 대신 **자세**를 표시 (누움/앉음/배회/걸터앉음/낙상/자리비움/뒤척임)
- **BED/FALL 동시 수신 시 심박·호흡이 화면에서 사라지던 버그 fix**
  - 원인: FALL 이 1초마다, BED 가 5~50초마다 오는데 카드는 "가장 최근 1건"만 보므로
    항상 FALL 이 이겨서 BED 의 심박·호흡·세밀한 자세가 영영 표시되지 않음
  - 수정: `analyzer._merge_radar_states()` 신규 — BED/FALL 최신 기록을 합쳐 하나의 현재 상태로.
    자세는 BED 우선(7종으로 세밀), 심박·호흡은 BED 에서, 낙상은 둘 중 하나라도 감지하면 낙상(안전 우선),
    120초 지난 기록은 무시해 옛날 낙상이 되살아나지 않게 함
- **원본 로그 파일 분리** — Emfit `emfit_data.jsonl` / AI Radar `radar_data.jsonl`
  - 한 파일에 섞으면 레이더(1초 주기, 하루 20MB+)가 커질수록 Emfit 조회까지 같이 느려진다.
    전체 재파싱은 서버 재시작·배정 변경 때마다 일어나므로 그동안 점검 페이지가 길어짐
  - `analyzer` 파싱 캐시를 **파일별 슬롯**으로 전환 — 한쪽에 새 줄이 붙어도 다른 쪽은 다시 읽지 않음.
    조회 시에는 두 파일을 합쳐서 하나의 storage 로 제공 (기기 SN 이 달라 키 충돌 없음)
  - `app.DATA_FILES` 에 파일 목록을 모아둠 — 기기 종류가 늘면 여기만 추가하면 된다
  - 원본 데이터 페이지(`/dashboard/raw`)는 두 파일의 줄 수를 합쳐 세고,
    가장 최근에 갱신된 파일의 마지막 줄을 보여준다
- **CSV 리포트를 기기 종류별로 분리**
  - Emfit 은 기존 파일명(`..._리포트.csv`) 유지, 레이더만 `..._Radar-BED.csv` / `..._Radar-FALL.csv`
    (같은 사람에게 두 기기를 배정하면 파일명이 겹쳐 ZIP 안에서 덮어쓰이던 문제도 해소)
  - 레이더 단일 날짜 다운로드는 BED/FALL 두 파일을 ZIP 으로. `?kind=bed|fall` 로 하나만도 가능
  - 레이더 기록에서 Emfit 전용 빈 컬럼(활동량/심박변이도) 제거, 형식별로 안 쓰는 컬럼도 자동 제외

## [3.2.2] - 2026-06-17
- **관리자가 그룹(A시설) 대시보드에 아예 못 들어가던 회귀 버그 fix**
  - 원인: 3.2.1에서 "admin이면 view 쿠키 무시"를 `_get_view_token_from_request`에 전역으로 걸었더니 `/view`(그룹 대시보드)·`/api/view/cards`까지 막혀 "접근할 수 있는 그룹이 없습니다"가 떴음
  - 수정: 쿠키 무시 로직을 제거해 admin도 그룹 대시보드를 볼 수 있게 되돌림. 3.2.1이 잡으려던 "전체 대시보드에서 그룹 기기 클릭 시 그룹으로 빨려감" 문제는 `/device/{sn}`의 `in_view` 판정으로 옮김 — admin은 **명시적 `?view=`** 로 들어왔을 때만 그룹 컨텍스트로 취급, 그룹 대시보드 카드 링크에 `?view=` 부착
- **그룹 대시보드 안에서 일부 기기 상세가 "접근 권한 없음(403)" 뜨던 버그 fix**
  - 원인: `_require_device_access`가 잔류 device 토큰 쿠키를 먼저 보고, 그 토큰이 가리키는 SN이 아니면 그룹 토큰을 확인하기도 전에 403("other device")으로 차단
  - 수정: device 토큰이 해당 기기를 허용하지 않으면 **곧장 막지 않고 view(그룹) 토큰을 먼저 확인**하도록 순서 변경
- **대시보드 속도 개선 (데이터 누적으로 느려지던 문제)**
  - 원인: `analyzer.get_latest_states()`가 카드 갱신(15초)마다 전체 storage(16만 줄 누적)를 풀스캔
  - 수정: 결과를 파일 mtime 기준으로 캐시 — 새 데이터가 들어왔을 때만 1회 재계산, 그 외에는 즉시 반환. 배정 이력 변경 시 캐시 무효화

## [3.2.1] - 2026-06-04
- **관리자가 그룹(A시설 등) 기기 상세에서 뒤로가기 시 그룹 대시보드로 빠지던 버그 fix**
  - 원인: 그룹 URL(`/v/<token>`)을 한 번 열면 `emfit_view` 쿠키가 365일 박힘 → admin이어도 `_get_view_token_from_request`가 잔류 쿠키를 주워 `in_view=True`가 되어 back_link가 `/view`로 향함
  - 수정: `_get_view_token_from_request` 에서 admin 로그인 상태면 쿠키 fallback 무시 (명시적 `?view=` 미리보기는 그대로 유지). 실사용자(view 토큰만 보유, admin 아님)는 영향 없음

## [3.2.0] - 2026-05-21
- **중간관리자(view 토큰)도 사용자명 수정 가능**
  - 권한 계층: 사업단(admin) → 중간관리자(view) → 실사용자(device) 자연스럽게 정리됨
  - `analyzer.update_active_user(sn, new_user)` — 활성 배정의 user 필드만 갱신 (location/group/start 등 보존). 종료된 옛 배정은 절대 안 건드림 (역사 기록 보존)
  - 새 라우트: `POST /devices/edit_user` — admin OR (view 토큰 + 자기 그룹 SN) 만 허용. device 토큰(실사용자)은 거부
  - 기기 상세 페이지(`/device/{sn}`) h1 옆에 ✏️ 이름 수정 버튼 (활성 배정만, admin·view 모두 노출). 클릭 → 인라인 폼 → 저장 → cache invalidate
  - 경고 문구: "이름 변경 시 이 활성 배정 기간 전체 데이터가 새 이름으로 표시됨 (옛 배정/리포트는 그대로)"

## [3.1.1] - 2026-05-21
- view(그룹) 토큰 사용자가 💬 의견 보내기 누르면 "권한 없다" 뜨던 버그 fix — `/feedback` GET/POST가 view 토큰도 허용
- 기기 상세 페이지(`/device/{sn}`)에서 admin이 view URL로 들어와 카드 클릭 후 "← 대시보드" 누르면 전체 5대(admin 대시보드)로 가던 버그 fix — view 컨텍스트면 `/view` 로 돌아감
- view 사용자의 device 상세 페이지에서 back link가 깨진 `/d/{view_token}` 가리키던 문제 fix

## [3.1.0] - 2026-05-21
- **그룹(view) 대시보드 도입**
  - 새 토큰 종류 `view_tokens.json` — `{token: {name, sns: [...]}}` 형식
  - `/v/{token}` 진입 → `/view` 그룹 대시보드 (그룹에 포함된 기기 카드만 노출)
  - `/api/view/cards` 갱신 엔드포인트
  - `/device/{sn}` 등 기기 상세도 그룹 토큰으로 접근 허용 (포함된 SN만, 다른 SN은 403)
  - `/admin/tokens` 페이지 하단에 그룹 발급 폼 (이름 + 체크박스로 기기 선택) + 기존 그룹 표 + 폐기 버튼
  - 그룹 view 페이지 메뉴: 사용 가이드 + 의견 보내기
- **URL 복사 버튼 fix**
  - `navigator.clipboard.writeText()` 가 HTTPS에서만 동작해서 HTTP 환경에서 silent fail 하던 문제
  - `document.execCommand('copy')` fallback 추가한 `copyText()` 헬퍼로 교체
  - 개별 + 그룹 URL 둘 다 적용

## [3.0.2] - 2026-05-20
- 대시보드 하단 버전 표시 색상 진하게 (`#cfd8dc` → `#90a4ae`)

## [3.0.1] - 2026-05-20
- `/dashboard` 하단에 현재 버전(`Emfit Server v{VERSION}`) 표시 추가
- `app.py` 상단에 `VERSION` 상수 도입

## [3.0.0] - 2026-05-19
- **배정 이력 시스템 도입** (`assignments.json`)
  - 기기(SN)별로 "누가·어디서·언제부터~언제까지" 기간 기록
  - `analyzer.resolve_assignment(sn, dt)` — 측정 시각으로 그때 사용자 판정
  - `/devices` 페이지 "🔄 기기 이전" 폼 + `POST /devices/handover`
  - `/reports` 페이지가 기기(SN) 대신 **배정(사람·기간) 단위**로 동작
  - 기기 상세 페이지 + 시계열 API도 배정 단위로 동작
- 사용자 그룹을 `analyzer.GROUPS` 한 곳에서 관리
- 재배포 시 86MB 재파싱 동안 점검 페이지 표시
- A시설 실증 시작 (2026-05-19 00:00부로 4대 재배치)

## [2.3.1] - 2026-05-11
- admin 세션 검사를 device 토큰 검사보다 먼저 수행하도록 fix
  (admin이 테스트로 `/d/{token}` 접속한 뒤 다른 기기 접근이 막히던 버그)

## [2.3.0] - 2026-05-11
- **대시보드 고도화**
  - 글로벌 datetime 범위 발췌 (`start_dt`/`end_dt` 파라미터, 자정 가로지름 가능)
  - 빠른 범위 버튼 (오늘 / 어제 / 지난 밤 / 최근 24시간)
  - 블록 순서 드래그(SortableJS) + ▲/▼ 버튼 재배치, viewer 단위 저장
  - x축 linear scale + epoch 좌표로 라벨 뭉침 fix, 5분 이상 갭 자동 끊김
- 차트별 시간 발췌 박스 제거 (글로벌 datetime이 역할 흡수)

## [2.2.0] - 2026-05-08
- 의견 상태 4단계 (미조치/조치중/조치완료/조치불가) + 관리자 답글 기능
- 상태/답글은 admin만 변경 가능, 모두 열람 가능

## [2.1.0] - 2026-05-08
- **UX 개선**
  - HR/RR/ACT 차트: X축 30분 간격 라벨, 가로 스크롤
  - 수면 Summary 복수 카드 표시 (낮잠/밤잠), 총수면 큰 순 정렬
  - 기기 상세 페이지에 실시간 현황 카드 + 가이드/의견 nav 추가
  - 로그인 페이지를 풀스크린 그라데이션 폼으로 변경

## [2.0.0] - 2026-05-08
- **인증 체계 도입** (기존 접근 방식 breaking)
  - 관리자: ID/PW 세션 로그인 (HMAC 쿠키 30일)
  - 피험자/가족: 개인 URL 토큰 `/d/{token}` — admin이 `/admin/tokens`에서 발급
  - 옛 admin 토큰 시스템 폐지
  - 토큰 발급 페이지에 내부(LAN) + 외부(DDNS) URL 동시 표시

## [1.1.0] - 2026-04-26
- `/devices` 기기 정보 편집 페이지 추가 (`device_info.json` 외부화)
- `/feedback` 의견 작성·열람 페이지 추가 (`feedback.jsonl`에 저장)
- HR/RR/ACT 글씨 크기 확대

## [1.0.0] - 2026-04-21~22
- **젯슨 이전** (윈도우 PC → Jetson Orin Nano)
  - 24/7 가동, 저전력 안정 운영
  - systemd 서비스 `emfit`, 파이썬 venv
  - DDNS `monitoring.example.com` 그대로 유지하면서 백엔드만 교체

## [0.2.0] - 2026-03 말 ~ 2026-04 초 *(추정)*
- `analyzer.py` 분석 로직 추가
- 일별 CSV 리포트 생성 시작 (첫 리포트 파일은 2026-03-23부터)

## [0.1.0] - 2026-03-18
- **초기 수신 서버 가동**
  - FastAPI `app.py` 첫 작성 (POST `/`, port 80, try/except 에러 처리, `host="0.0.0.0"`)
  - `emfit_data.jsonl` JSONL 형식 저장
- **네트워크 인프라 구성**
  - Windows 방화벽 인바운드 룰 (TCP 80 허용, 이름 `Emfit_API`)
  - TP-Link 공유기 포트 포워딩 (외부 80 → 내부 SERVER_LAN_IP:80)
  - TP-Link DDNS 발급: `monitoring.example.com`
  - Emfit 본사(Pekka)에 API endpoint URL 변경 요청

## [0.0.1] - 2026-03-18
- FastAPI 첫 코드 작성 — `/data-endpoint`, port 8000, JSON 받아서 `print`만
- 2026-03-16에 finenurse 측에서 API 컨테이너 생성 (`http://partner.example.com/`)
